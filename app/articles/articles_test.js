'use strict';

describe('readerApp.articles module', function() {

  beforeEach(module('readerApp.articles'));

  describe('articles controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var articlesCtrl = $controller('ArticlesCtrl');
      expect(articlesCtrl).toBeDefined();
    }));

  });
});